name: Assign issues to Jezza34000

on:
  issues:
    types:
      - opened
      - reopened

permissions:
  issues: write

jobs:
  assign:
    runs-on: ubuntu-latest
    steps:
      - name: Assign issue to Jezza34000
        uses: actions-ecosystem/action-add-assignees@a73fcabd82d847c5e7433fcfdd58ef9f7e8a3993 # Version 1.0.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          assignees: Jezza34000
